<div class='container'>
  <h3>注文履歴一覧</h3>
  <div class ='row'>
    <div class = "border table-striped table table-hover">
      <table class = "col-md-12">
          <thead>
            <tr>
              <th>購入日時</th>
              <th>購入者</th>
              <th>注文個数</th>
              <th>注文ステータス</th>
            </tr>
          </thead>
          <tbody>
            <% @orders.each do |order| %>
            <tr>
              <td><%= link_to admin_order_path(order.id) do %>
                  <%= l order.created_at %>
                  <% end %>
              </td>
              <td><%= order.name %></td>
              <td>
                <% @amount_sum = 0 %>
                <% order.order_details.each do |detail| %>
                <% @amount_sum += detail.quantity %>
                <% end %>
                <%= @amount_sum %>
              </td>
              <td><%= order.order_status_i18n %></td>
            </tr>
            <% end %>
          </tbody>
      </table>
    </div>
    <div>
       <div class="mx-auto" style="width: 200px;"><%= paginate @orders %></div>
    </div>
  </div>
</div>