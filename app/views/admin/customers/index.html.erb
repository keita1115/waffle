<div class='container'>
  <div class='row'>
    <div class='col-md-8 offset-2'>
      <h3 class="my-4 mt-5">会員一覧</h3>

      <table class="table table">
        <thead>
          <tr>
            <th>会員ID</th>
            <th>氏名</th>
            <th>メールアドレス</th>
            <th>ステータス</th>
          </tr>
        </thead>
        <tbody>
          <% @customers.each do |user| %>
            <tr>
              <td>
                <%= user.id %>
              </td>
              <td>
              <%= link_to admin_customer_path(user) do%>
                <%= user.last_name %><%= user.first_name %>
              <% end %>
              </td>
              <td><%= user.email%></td>
              <% if user.is_valid == '有効'  %>
          	    <th class = "text-success">
          	      <%= user.is_valid %>
          	    </th>
          	  <%else%>
          	    <th class = "text-danger">
          	      <%= user.is_valid %>
          	    </th>
          	  <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
        <p><%= paginate @customers %></p>
    </div>
  </div>